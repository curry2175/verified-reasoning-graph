# v004 변경사항

v004의 목표는 **Verifier가 왜 그런 판정을 내렸는지 사용자가 직접 확인할 수 있게 하는 것**입니다.

## 새 기능

- 각 Node의 SMT-LIB 표현 표시
- Z3가 실행한 두 검사 표시
  - `K ∧ F`: 현재 주장이 기존 지식과 모순되는지 검사
  - `K ∧ ¬F`: 현재 주장이 기존 지식에서 반드시 따라오는지 검사
- 각 검사의 `SAT / UNSAT / UNKNOWN / NOT_RUN` 결과 표시
- Z3 raw unsat core와 삭제 기반 최소화 이후 근거를 분리 표시
- `Z3 증명 경로`라는 이름을 `Z3가 선택한 증명 근거`로 수정
- Horn forward chaining으로 찾은 다른 증명 경로를 최대 3개 표시
- 영향도 설명을 다음처럼 분리
  - 원래 LLM 경로가 끊기는가
  - 원래 경로에서 몇 Node가 영향을 받는가
  - 다른 경로로 Chain을 복구할 수 있는가
  - Node를 삭제해도 논리적 최종 답이 유지되는가
- 독립적인 두 증명 경로를 가진 `sample_multiple_proofs` 추가

## 주의사항

- `Z3가 선택한 증명 근거`는 가능한 모든 증명을 뜻하지 않습니다.
- 대체 경로 목록은 현재 지원하는 finite Horn 규칙으로 찾은 지원 집합이며 최대 3개만 표시합니다.
- Z3 unsat core는 원래 최소가 아닐 수 있어 v004가 삭제 기반 최소화를 추가로 수행합니다.
- 현재도 Node 수정 후에는 전체 Graph를 재검증합니다. 부분 재검증은 이후 버전에서 구현할 예정입니다.
